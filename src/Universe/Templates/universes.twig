{% extends "@App/layout/page-layout.twig" %}

{% block content %}
    <section class="section">
        <div class="container">
            <h1 class="title">{{ 'universe.my.universes' | trans }}</h1>
            <div class="columns is-desktop">
                {% for universe in universes if universe.isActive() %}
                    <div class="column">
                        {% include '@Universe/universe-card.twig' %}
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>
{% endblock %}
