{% extends "@App/layout/game-layout.twig" %}

{% block content %}
    {% include "@App/layout/partial/breadcrumb.twig"
        with {
            'items': [
                {'name': currentUniverse.getName, 'icon': 'fas fa-atom has-text-primary'},
            ]
        }
    %}

    {% include "@App/layout/partial/page-title.twig"
        with {
            'title': 'player.overview' | trans,
            'subtitle' : 'universe.dashboard.description' | trans,
        }
    %}
{% endblock %}